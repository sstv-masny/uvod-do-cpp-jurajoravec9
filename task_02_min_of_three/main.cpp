
#include <iostream>
using namespace std;
int main() {
    long long a, b, c;
    cout << "Zaddaj tri cisla";
    cin >> a >> b >> c;
    if (b < min) {
        min = b;
    }
    if (c < min) {
        min = c;
    }
    cout << "najmensie c je " << min;
    return 0;
}
